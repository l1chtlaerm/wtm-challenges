function conversion(array, callback = undefined) {

};

function keyConverter(i) {
    return `key#${i}`
};

function keyMultiplier(i) {
    return i * 2
};

module.exports = { conversion, keyConverter, keyMultiplier }; // No remover.
